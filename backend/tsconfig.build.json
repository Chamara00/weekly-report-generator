{
  "extends": "./tsconfig.json",
  "//": "prisma/ is excluded so the seed script does not widen rootDir and push the app output to dist/src. The seed runs through ts-node, never through nest build.",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "prisma"]
}
